# Pinned to current known-good versions for reproducible builds (was unpinned >=,
# a latent build-break risk — each build otherwise pulls latest). Bump deliberately.

# Core dependencies
langchain==1.3.10
langchain-openai==1.3.2
chromadb==1.5.9
openai==2.43.0

# Document processing
pypdf==6.13.3
python-docx==1.2.0

# API
fastapi==0.138.0
uvicorn==0.49.0
python-multipart==0.0.32

# HTTP client for Google Drive
requests==2.34.2

# Environment
python-dotenv==1.2.2

# Observability / tracing (Phase 5) — pinned to match workflow-provider.
# Optional at runtime: memory_unit/tracing.py imports it behind try/except and
# only traces when CONFIDENT_API_KEY is set, so a missing/failed import is safe.
deepeval==4.0.6

# Data validation
pydantic==2.13.4

# Utils
typing-extensions==4.15.0
